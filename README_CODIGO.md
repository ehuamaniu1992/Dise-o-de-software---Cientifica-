# Código Semana 1

1. Cree un entorno virtual: `python -m venv .venv`.
2. Active el entorno.
3. Instale dependencias si existen: `pip install -r requirements.txt`.
4. Ejecute el archivo base.
5. Modifique el código según el caso práctico.
